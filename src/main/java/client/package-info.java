/**
 * 
 */
/**
 * @author USER
 *
 */
package client;

/*login sequence

0. check whether Id is in deviceMap

1. set device to client

2. send login reply message

3. send server data to client
	
	if device = android
	
	send connected device data(ID, Type, connection ...)
	
	else
	
	send android flag
	
4. notify login device to other clients








*/